name: Re-deploy to prevent Notion 24h image expiration

on:
  schedule:
    - cron: '0 */12 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger re-deployment
        run: |
          curl --request POST \
          --url '${{ secrets.VERCEL_DEPLOYMENT_HOOK }}'
